{
  "name": "@lautr/initium-nuxt-webapp",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "preview": "nuxt preview",
    "dev": "nuxi dev",
    "start": "nuxi start",
    "coverage": "vitest run --coverage",
    "postinstall": "nuxi prepare",
    "test": "vitest run",
    "ci:test": "vitest run",
    "ci:smoke": "pnpm dlx concurrently -k -s first -n \"NUXT,TEST\" -c \"magenta,blue\" \"pnpm dev\" \"pnpm dlx wait-on tcp:3000 && pnpm echo \"pass\" \""
  },
  "devDependencies": {
    "@lautr/initium-nuxt-design": "workspace:*",
    "@nuxt/devtools": "catalog",
    "@nuxt/test-utils-edge": "catalog",
    "@nuxtjs/color-mode": "catalog",
    "@pinia/nuxt": "catalog",
    "@tailwindcss/vite": "catalog",
    "@vue-macros/nuxt": "catalog",
    "@vueuse/components": "catalog",
    "@vueuse/core": "catalog",
    "@vueuse/nuxt": "catalog",
    "autoprefixer": "catalog",
    "nuxt": "catalog",
    "pinia": "catalog",
    "tailwindcss": "catalog",
    "vitest": "catalog"
  }
}
