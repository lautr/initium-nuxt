import { Canvas, Meta, Story } from "@storybook/addon-docs";

import UiSwHeader from "~/components/ui/SwHeader.vue";

<Meta
  title="SW/Header"
  component={UiSwHeader}
  argTypes={{
    placeholder: {
      name: "placeholder",
      type: { name: "string", required: false },
      defaultValue: "Hello"
    }
  }}
/>

# Input

A input component

<Canvas>
  <Story name="UiSwHeader">
    {args => {
      return {
        setup() {
          return { args };
        },
        components: { UiSwHeader },
        template: `
          <UiSwHeader v-bind="args" />`
      };
    }}
  </Story>
</Canvas>
