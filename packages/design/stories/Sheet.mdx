import { Meta, Canvas } from '@storybook/blocks';

import * as UiStories from './Sheet.stories';

## Sheet

A sheet component, provides a sheet to display content.

<Meta of={UiStories} />
<Canvas of={UiStories.Sheet} />