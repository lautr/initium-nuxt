import { Canvas, Meta, Story } from "@storybook/addon-docs";
import UiSheet from "~/components/ui/Sheet.vue";

<Meta title="UI/Sheet" component={UiSheet} />

# Sheet

A sheet component, provides a sheet to display content.

<Canvas>
  <Story name="Sheet">
    {args => {
      return {
        setup() {
          return { args };
        },
        components: { UiSheet },
        template: `
          <UiSheet v-bind="args">Content</UiSheet>`
      };
    }}
  </Story>
</Canvas>
